#include("src/chipper.stanza")
#use-syntax(core, chipper)

defpackage dualgcd :
   import core
   import verse
   import chipper

defbundle Dual<T> :
   a: T
   b: T

defmodule GCD :
   input in: Dual<UInt<16>>
   input e: UInt<1>
   output z: UInt<16>
   output v: UInt<1>

   reg x: UInt
   reg y: UInt
   when x > y :
      x := x - y
   else :
      y := y - x
   when e :
      x := in.a
      y := in.b
   z := x
   v := y === UInt(0)

defn main () :
   circuit c of GCD
   println(c)
   
main()
